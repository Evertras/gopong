# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Our output binary 
gopong

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Can be created by dep when an issue happens while updating, we definitely don't want this
.vendor-new

# Protobuf compilation
messages/*message

# Output of generate
lib/static/build.go

# Javascript stuff
node_modules/
front/build

# Build artifact for Javascript
front/game.js
front/game.js.map
